{
  "name": "deadman-structure-monorepo",
  "private": true,
  "workspaces": [
    "apps/api",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  }
}
